есть сообщество дома
├── в нем состоят участники (обычные + старшие и казначеи выбранные из обычных)
    ├── они (каждый согласно своей роли) что-то делают:
        ├── обычные участники:
            выдвигаются кандидатом (на старшего или казначея)
            формируют рейтинг (старшего или казначея)
            избирают (старшего или казначея)
            добавляют инициативу
            плюсуют или минусят (жителей или инициативу)
            голосуют (за или против инициативы)
            добавляют сбор
            отправляют деньги на сбор
            контроллируют зачисление (видят все поступления учтеные Казначеем)

        ├── старший по дому (все тоже самое что и обычный участник +):
            верифицирует участника
            может самоустраниться
            бан заминусеных участников (за их инициативы, сообщения)
            определяет объявлять сбор на инициативу или в рамках УО реализовывать
        ├── казначей (все тоже самое что и обычный участник +):
            подтверждает прием средств
            делает возврат
            продляет сбор
            выдает деньги
        ├── при этом создаются сущности:
            ├── избирательное голосование
            ├── голосование за инициативу
            ├── казна сообщества